<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-path
        name="external_files"
        path="." />

    <!-- Auto-update downloads land in getCacheDir()/updates/ and are
         handed to the system installer via FileProvider. -->
    <cache-path
        name="updates"
        path="updates/" />
</paths>
